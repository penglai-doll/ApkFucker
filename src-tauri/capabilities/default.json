{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "桌面默认能力，允许 shell sidecar 与原生文件选择",
  "windows": ["main"],
  "permissions": ["core:default", "shell:allow-execute", "dialog:allow-open"]
}
