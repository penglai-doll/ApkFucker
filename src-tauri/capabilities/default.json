{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability for shell execution",
  "windows": ["main"],
  "permissions": ["core:default", "shell:allow-execute"]
}
